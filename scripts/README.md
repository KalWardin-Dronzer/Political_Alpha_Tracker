# Ad-hoc probes

One-off scripts used to poke at a data source or a single function while debugging. They are NOT tests — they were named `test_*.py` at the repo root, where pytest tried to collect them. Kept because they document how each source was verified by hand.
